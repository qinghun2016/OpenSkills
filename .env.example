# 环境变量示例文件
# 复制此文件为 .env 并填入实际值

# ==================== 应用配置 ====================
NODE_ENV=development
# API 端口：npm 手动启动时默认 3000；插件模式默认 3847
PORT=3000

# ==================== API 配置 ====================
# API 基地址：脚本与 Agent 使用 OPENSKILLS_API_URL
# 插件模式默认 http://localhost:3847，npm 启动默认 http://localhost:3000
OPENSKILLS_API_URL=http://localhost:3847
API_BASE_URL=http://localhost:3847
CORS_ORIGIN=http://localhost:5173

# ==================== 工作区配置 ====================
# 工作区根目录，供 API 与脚本解析 .openskills、.cursor 路径
# WORKSPACE_ROOT=
# WORKSPACE=

# ==================== Web 配置 ====================
# Vite 代理目标与端口：插件模式 API 3847、Web 3848
VITE_API_URL=http://localhost:3847
VITE_PORT=3848

# ==================== GitHub 配置 ====================
# GitHub Personal Access Token (用于爬虫功能)
# 创建方式: https://github.com/settings/tokens > Generate new token (classic)
# 所需权限: public_repo
# 注意: 请勿将 token 写入 .openskills/config.json，仅使用本环境变量
GITHUB_TOKEN=

# ==================== 日志配置 ====================
LOG_LEVEL=info
LOG_FORMAT=json

# ==================== 安全配置 ====================
# JWT Secret (如果使用认证)
# JWT_SECRET=your-secret-key-here

# Session Secret (如果使用 session)
# SESSION_SECRET=your-session-secret-here

# ==================== 数据库配置 (可选) ====================
# 如果未来需要数据库支持
# DATABASE_URL=postgresql://user:password@localhost:5432/openskills
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10

# ==================== Redis 配置 (可选) ====================
# 如果需要缓存支持
# REDIS_URL=redis://localhost:6379
# REDIS_TTL=3600

# ==================== 监控配置 (可选) ====================
# Sentry DSN (错误追踪)
# SENTRY_DSN=

# Google Analytics ID
# GA_TRACKING_ID=

# ==================== 邮件配置 (可选) ====================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ==================== 文件存储 (可选) ====================
# AWS S3 配置
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=

# ==================== 其他配置 ====================
# 开启调试模式
DEBUG=false

# 请求体大小限制 (单位: MB)
MAX_REQUEST_SIZE=10

# 上传文件大小限制 (单位: MB)
MAX_UPLOAD_SIZE=50
