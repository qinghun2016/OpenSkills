# 🔌 VS Code 插件安装指南

## ✅ 插件已经编译完成！

插件代码已经在 `packages/extension/out/` 目录中编译完成。

## 🚀 推荐方式：开发模式（最简单）

**无需安装，直接测试！**

### 步骤：

1. **在当前 VS Code 窗口中按 `F5`**
   - 或者点击菜单：`Run > Start Debugging`
   - 或者点击左侧的调试图标，选择"运行扩展"

2. **等待新窗口打开**
   - 会打开一个新的 VS Code 窗口
   - 标题显示 `[扩展开发主机]`

3. **在新窗口中查看**
   - 左侧边栏会出现 "OpenSkills" 区域
   - 包含两个视图：
     - **OpenSkills** - Skills 列表
     - **Proposals** - 提议列表

4. **测试功能**
   - 按 `Ctrl+Shift+P` 打开命令面板
   - 输入 "OpenSkills" 查看所有命令：
     - `OpenSkills: Open Web UI` - 打开 Web 界面
     - `OpenSkills: Refresh` - 刷新列表
     - `OpenSkills: Approve Proposal` - 批准提议
     - `OpenSkills: Reject Proposal` - 拒绝提议
     - `OpenSkills: Initialize` - 初始化项目
     - `OpenSkills: Trigger Wake` - 手动唤醒

---

## 📦 方式二：正式安装（如果需要）

如果你想正式安装插件：

### 选项 A: 使用 npx（推荐）

```bash
cd packages/extension
npx @vscode/vsce package
```

这会生成 `openskills-0.1.0.vsix` 文件。

### 选项 B: 本地安装 vsce

```bash
cd packages/extension
npm install --save-dev @vscode/vsce
npx vsce package
```

### 安装 .vsix 文件

1. 在 VS Code 中按 `Ctrl+Shift+P`
2. 输入 "Extensions: Install from VSIX"
3. 选择生成的 `openskills-0.1.0.vsix` 文件
4. 重启 VS Code

---

## 🎨 插件功能介绍

### 侧边栏视图

安装后，左侧边栏会显示：

#### 1. OpenSkills 视图
- 显示所有已安装的 Skills
- 分为用户级和项目级
- 可以查看 Skill 详情

#### 2. Proposals 视图  
- 显示所有提议
- 按状态分类（pending/approved/rejected）
- 支持右键操作（批准/拒绝）

### 命令面板命令

按 `Ctrl+Shift+P` 可用的命令：

| 命令 | 功能 |
|------|------|
| `OpenSkills: Initialize` | 初始化项目 |
| `OpenSkills: Open Panel` | 打开管理面板 |
| `OpenSkills: Open Web UI` | 在浏览器打开 Web 界面 |
| `OpenSkills: Refresh` | 刷新列表 |
| `OpenSkills: Approve Proposal` | 批准选中的提议 |
| `OpenSkills: Reject Proposal` | 拒绝选中的提议 |
| `OpenSkills: Trigger Wake` | 手动触发唤醒 |

### 右键菜单

在 Proposals 视图中右键点击 pending 状态的提议：
- ✅ **Approve** - 批准提议
- ❌ **Reject** - 拒绝提议

---

## 🔧 配置

**这些配置均在 VS Code/Cursor 设置中**（`Ctrl+,` → 搜索 "OpenSkills"），**不在** `.openskills/config.json`。

按 `Ctrl+,` 打开设置，搜索 "OpenSkills"：

```json
{
  "openskills.apiUrl": "http://localhost:3847",
  "openskills.autoRefresh": true,
  "openskills.refreshInterval": 30,
  "openskills.useAgentCliForSkills": true,
  "openskills.wakeUseAgentCli": true,
  "openskills.wakeAgentPrompt": ""
}
```

| 设置 | 说明 | 默认值 |
|------|------|--------|
| `openskills.apiUrl` | API 服务地址（插件内嵌服务默认 3847） | `http://localhost:3847` |
| `openskills.autoRefresh` | 自动刷新提议列表 | `true` |
| `openskills.refreshInterval` | 刷新间隔（秒） | `30` |
| `openskills.useAgentCliForSkills` | 是否用 Cursor Agent CLI 创建/管理 skills；禁用则用直接创建 | `true` |
| `openskills.wakeUseAgentCli` | 触发唤醒时是否用 Cursor Agent CLI 启动 Agent；否则仅调 API + 提示 | `true` |
| `openskills.wakeAgentPrompt` | 唤醒 Agent 的 prompt；为空则用默认 | `""` |

---

## 🎯 快速开始

### 最简单的方式：

1. **按 `F5`** - 启动扩展开发模式
2. **等待新窗口打开**
3. **查看左侧边栏** - OpenSkills 和 Proposals 视图
4. **按 `Ctrl+Shift+P`** → 输入 "OpenSkills" 测试命令

---

## 🐛 故障排查

### 问题 1: 按 F5 没反应

**解决方案**:
- 确保 `.vscode/launch.json` 文件存在（已创建）
- 点击左侧调试图标，选择"运行扩展"
- 点击绿色播放按钮

### 问题 2: 扩展无法连接 API

**解决方案**:
1. 确保 API 服务正在运行
2. 访问实际 API 地址的 /health（如 http://localhost:3847/health）验证
3. 检查 VS Code 设置中的 `openskills.apiUrl`

### 问题 3: 侧边栏没有 OpenSkills 视图

**解决方案**:
- 在新窗口（扩展开发主机）中查看
- 不是在启动调试的原窗口

### 问题 4: 命令找不到

**解决方案**:
- 等待扩展完全激活（几秒钟）
- 重新打开命令面板 `Ctrl+Shift+P`

### 问题 5: 找不到 `openskills.useAgentCliForSkills` 等配置项

**说明**：这些配置在 **VS Code/Cursor 设置** 中，**不在** `.openskills/config.json`。

**解决方案**:
1. 按 `Ctrl+,` 打开设置，在搜索框输入 **"OpenSkills"**（不要只搜 `useAgentCliForSkills`）
2. 确认 **OpenSkills 扩展已加载**：开发模式下在 **扩展开发主机** 窗口（F5 打开的新窗口）里改设置；正式安装后在本机设置中即可见
3. 也可在 `settings.json` 中手动添加，例如：
   ```json
   "openskills.useAgentCliForSkills": false,
   "openskills.wakeUseAgentCli": true
   ```

---

## 💡 开发技巧

### 调试扩展

1. 在 `packages/extension/src/extension.ts` 设置断点
2. 按 `F5` 启动调试
3. 在扩展开发主机中触发功能
4. 断点会在原窗口暂停

### 查看日志

在原 VS Code 窗口：
- 打开调试控制台（`Ctrl+Shift+Y`）
- 查看 `console.log` 输出

在扩展开发主机窗口：
- 打开输出面板（`Ctrl+Shift+U`）
- 选择 "扩展主机" 查看日志

### 重新加载

修改代码后：
1. 在扩展开发主机窗口按 `Ctrl+R`
2. 或点击调试工具栏的重启按钮 🔄

---

## ✨ 现在就开始！

**按 `F5` 键启动，立即体验 OpenSkills 插件！**

不需要安装、不需要打包，直接就能用！
